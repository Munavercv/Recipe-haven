<section>
    <div class="container mt-5">
        <div class="row">

            <h1>this is user home</h1>

            {{!-- {{#each products}}
            <div class="col-md-3 my-3 p-3">
                <div class="card">
                    <img src="/product-images/{{this._id}}.jpg" class="card-img-top" alt="iphone 15">
                    <div class="card-body  d-flex flex-column">
                        <h5 class="card-title">{{this.name}}</h5>
                        <p class="card-text">{{this.description}}</p>
                        <p class="card-text fw-bold">&#8377;{{this.price}}</p>
                        <button onclick="addToCart('{{this._id}}')" class="btn btn-primary mt-auto">Add to cart</button>
                    </div>
                </div>
            </div>
            {{/each}} --}}

        </div>
    </div>
</section>